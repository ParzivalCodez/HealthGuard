import { StyleSheet } from "react-native";

const LoginStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
  },
  MapContainer: {
    height: "100%",
    width: "100%",
    flex: 1,
  },
});

export default LoginStyles;
